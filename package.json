{
    "name": "StudentCRUD",
    "scripts": {
        "dev": "bun run --hot src/index.ts",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:coverage": "vitest run --coverage"
    },
    "dependencies": {
        "hono": "^4.12.8",
        "zod": "^4.3.6"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "vitest": "^4.1.0"
    }
}
